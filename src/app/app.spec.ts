/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { AppComponent } from './app.component';

describe('App: Clashroyaledeck', () => {
  beforeEach(() => {
    addProviders([AppComponent]);
  });

});
